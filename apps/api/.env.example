# App environment: "development" or "production"
APP_ENV=development

# Server listen address
API_ADDR=:8080

# Allowed web origins (comma-separated for multiple)
WEB_ORIGINS=http://localhost:3000

# Base URL for magic links (defaults to first WEB_ORIGINS value)
APP_URL=http://localhost:3000

# Auth token HMAC secret (CHANGE IN PRODUCTION: openssl rand -hex 32)
AUTH_TOKEN_SECRET=dev-secret-change-me

# SQLite database path (leave empty for in-memory store)
# DATABASE_URL=/data/lumen.db

# Resend email provider (leave empty for dev console logging)
# RESEND_API_KEY=re_...
# RESEND_FROM_EMAIL=Lumen <lumen@yourdomain.com>

# OpenTelemetry (optional)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
